{
  "name": "domily",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build:rc": "bun --filter @domily/runtime-core build",
    "build:dr": "bun --filter @domily/router build",
    "build": "bun build:rc && bun build:dr",
    "test:vp": "bun --filter vite-plugin-domily test",
    "build:vp-test": "bun --filter vite-plugin-domily test:build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}